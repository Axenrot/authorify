const Brokers = () => {
  return (
    <section className="relative flex-col h-[100vh] w-full flex items-center bg-white shadow-xl shadow-black justify-center text-black">
      <div className="px-3 items-center container mx-auto h-full blur-none py-24 flex flex-col gap-12">
        {/* Brokers message */}

        <h2 className="font-serif flex flex-col text-3xl lg:text-5xl font-semibold items-center lg:items-start justify-center">
          Authorify is trusted by the best brokers in the industry.
        </h2>

        {/* Brokers image */}
      </div>
    </section>
  );
};

export default Brokers;
