const Hero = () => {
  return (
    <section className="h-[100vh] border border-red-500 w-full flex items-center justify-center bg-[url('/images/header-bg-transformed.jpeg')] bg-cover">
      Hello World
    </section>
  );
};

export default Hero;
